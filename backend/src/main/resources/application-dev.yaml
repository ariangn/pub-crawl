spring:
  datasource:
    url: jdbc:mysql://localhost:8889/pubcrawl_api?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
# websiteUrl: 